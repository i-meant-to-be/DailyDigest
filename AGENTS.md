# AGENTS

이 저장소에서 Claude가 지켜야 할 실행 규칙. 사용자 요청과 `CLAUDE.md`가 우선하며, 이 문서는 저장소 고유의 제약만 정의한다.

## 1. 지식 원문 보호

`knowledge/` 아래 문서는 **사용자가 직접 작성한 학습 기록**이다.

- 사실 오류, 오탈자, 중복을 발견해도 **승인 없이 고치지 않는다.** `docs/AUDIT.md`에 항목을 추가하고 사용자에게 알린다.
- 예외는 둘뿐이다. (1) `docs/AUDIT.md`에서 승인 표시된 항목, (2) 채점을 마친 `gap` 슬롯 문항을 새 ID로 등록하는 경우.
- 문항 ID는 **한 번 부여하면 재사용하지 않고 번호를 다시 매기지 않는다.** 항목을 다른 파일로 옮겨도 ID는 따라간다. 접두사가 현재 파일과 달라져도 그대로 둔다.
- 문항을 삭제하지 않는다. 중복 병합조차 승인 사항이다.

## 2. 상태 파일

`state/mastery.json`과 `state/sessions.json`은 **`scripts/digest.mjs`를 통해서만 수정한다.** 직접 편집하지 않는다.

이유는 두 가지다. 상태 전이·감쇠·집계 규칙이 코드에 있어 손으로 고치면 규칙과 어긋나고, 파일 전체를 컨텍스트에 올리면 문항이 늘수록 감당이 안 된다.

- 상태 갱신의 근거는 **채점 결과뿐이다.** 대화 중에 사용자가 뭔가를 잘 아는 것 같아 보인다는 이유로 상태를 올리지 않는다.
- 답변이 비어 있는 문항은 0점이 아니라 **미응답**이다. 상태도 `attempts`도 건드리지 않는다.
- `state/REPORT.md`는 생성물이다. 손으로 쓰지 않는다.

## 3. 채점

- **면접관 기준으로 채점한다. 좋게 봐주지 않는다.** 4점은 실제 면접에서 통과할 답에만 준다.
- 정오 판정으로 끝내지 않는다. **왜 틀렸는지, 무엇이 빠졌는지, 면접에서는 어떻게 말했어야 하는지**를 쓴다.
- 사용자 답이 원문보다 정확하거나 원문의 오류를 드러낸 경우 — 조용히 고치지 말고 `docs/AUDIT.md`에 올린 뒤 채점 결과 말미에 알린다.

## 4. 컨텍스트 절약

- 미채점 세션은 `digest.mjs pending`으로 찾는다. `sessions/`를 훑거나 질문지 본문을 열어 보지 않는다.
- 출제 시 `digest.mjs select`의 출력과 **선정된 문항의 원문만** 읽는다. `mastery.json` 전체를 읽지 않는다.
- 과목별 숙련도가 필요하면 `digest.mjs report`를 돌린다. JSON을 읽고 직접 세지 않는다.

## 5. 문서 작성

- 한국어로 쓴다. 식별자·명령어·제품명을 제외하고는 불필요한 영문 용어를 피한다.
- 커밋은 의미 단위로 나누고 `label: message` 형식을 쓴다.

## 6. 다이제스트 결과 게시

`digest` 스킬이 만든 변경은 **채점을 마친 뒤에 한 번만** 커밋하여 현재 브랜치에 설정된 원격 업스트림으로 푸시한다. 이는 다이제스트 실행에 한정된 커밋·푸시 권한이며 다른 작업으로 확대하지 않는다.

- **출제만 한 시점에는 커밋하지 않는다.** 질문지와 그때 갱신된 상태 파일은 작업 트리에 그대로 두고, 사용자가 답을 적어 채점이 끝날 때까지 기다린다.
- 채점을 마치면 **출제 때 생긴 변경과 채점 때 생긴 변경을 하나의 커밋으로 묶는다.** 같은 세션의 결과를 두 커밋으로 나누지 않는다.
- 상태 조회(`/digest status`)만으로 생긴 `state/REPORT.md` 갱신은 그것만으로 커밋하지 않는다. 다음 채점 커밋에 함께 실린다.
- 커밋 직전 실제 브랜치와 업스트림, 작업 트리와 staging 영역을 확인한다.
- 기존 사용자 변경이나 해당 다이제스트 세션과 무관한 파일은 staging하거나 커밋하지 않는다. 둘을 안전하게 분리할 수 없으면 커밋하지 말고 사용자에게 알린다.
- 커밋 메시지는 커밋 시점의 `Asia/Seoul` 시간을 사용해 정확히 `digest: YYYY-MM-DD-HHmm` 형식으로 작성한다.
- 변경이 없으면 빈 커밋을 만들지 않는다.
- 업스트림이 없거나 예상하지 못한 브랜치·원격을 가리키면 임의로 설정하거나 푸시하지 말고 사용자에게 알린다.
- 푸시가 실패하면 성공했다고 보고하지 않으며, 원인을 확인하지 않은 채 같은 커밋이나 상태 변경을 반복하지 않는다.
